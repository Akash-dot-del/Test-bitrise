format_version: '11'
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

workflows:
  primary:
    steps:
      - script:
          title: Set executable permissions
          inputs:
            - content: |
                #!/bin/bash
                git clone https://github.com/Akash-dot-del/Test-bitrise.git
      - script:
          title: Set executable permissions
          inputs:
            - content: |
                #!/bin/bash
                cd /subdir
      - script:
          title: Set executable permissions
          inputs:
            - content: |
                #!/bin/bash
                cd Test-bitrise
      - script:
          title: Run ./sharp
          inputs:
            - content: |
                #!/bin/bash
                ./sharp
      - script:
          title: Run Python script
          inputs:
            - content: |
                #!/bin/bash
                python3 k.py
                
